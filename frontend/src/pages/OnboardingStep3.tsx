/**
 * Onboarding Step 3 Component
 * 
 * Third step of the onboarding process - Services, Categories & Defaults.
 * This component is now fully implemented with service catalog creation.
 */

import React from 'react';
import { Step3Services } from './onboarding/Step3Services';

export const OnboardingStep3: React.FC = () => {
  return <Step3Services />;
};
